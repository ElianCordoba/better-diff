{
  "scripts": {
    "test": "vitest --dir ./tests",
    "build": "tsc --watch",
    "check": "npm run format && npm run lint",
    "format": "deno fmt --options-line-width 300",
    "lint": "deno lint --ignore=node_modules,diff,internal,dist,tests,test.ts",
    "startServer": "tsx src/server.ts"
  },
  "devDependencies": {
    "vite": "4.0.3",
    "vitest": "0.26.2"
  },
  "dependencies": {
    "@fastify/cors": "^8.2.0",
    "fastify": "^4.10.2",
    "kleur": "^4.1.5",
    "typescript": "5.0.0-dev.20221224"
  }
}